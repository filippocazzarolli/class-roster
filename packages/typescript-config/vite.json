{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./react-internal.json",
  "compilerOptions": {
    /* L'unica differenza rispetto a un pacchetto interno: qui gira Vite, e i suoi tipi
       servono (`import.meta.env`, gli import di asset). */
    "types": ["vite/client"]
  }
}
